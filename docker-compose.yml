version: '3'
services:
  jobber_dev:
    image: postgres
  jobber:
    build: .
    command: bundle exec rails s -p 3000 -b '0.0.0.0'
    volumes:
      - .:/Jobber
    ports:
      - "3001:3000"
    depends_on:
      - jobber_dev
